!!! 5
%html(lang="en")
  %head
    = render 'layouts/head'
    = #yield :head

  %body
    #header
      = render 'layouts/header'

    .container-fluid
      .row
        .col-md-12
          = yield

    - #cache('application/footer') do
    = render 'layouts/footer'
    = render 'layouts/js'
    = #yield :scripts
